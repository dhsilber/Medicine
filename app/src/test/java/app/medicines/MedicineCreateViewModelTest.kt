package app.medicines

import app.medicines.data.MedicineRepository
import app.medicines.ui.MedicineCreateViewModel
import io.mockk.mockk
import org.junit.Test

class MedicineCreateViewModelTest {
    @Test
    fun `Create Medicine from name`() {
//        val mockRepository = mockk(MedicineRepository)
//        val viewModel = MedicineCreateViewModel()
    }
}